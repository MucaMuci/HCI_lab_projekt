const Note = ({text}) => (
    <div className=" pl-5 flex flex-row justify-self-center self-center w-full">
        <div className="flex flex-wrap">
            {text}
        </div>
    </div >
);

export  default Note;
